
WebAssembly 기반 Linux(linux-wasm)로 리눅스 학습용 웹사이트를 만든다. 사용자는 튜토리얼을 생성·저장하고, 고유 링크로 누구와도 공유할 수 있다. 튜토리얼 화면 레이아웃은 좌측 ‘문서’ 패널, 우측 linux-wasm 실행 패널로 고정한다. 사용자는 로그인할 필요는 없다. 사용성은 누구보다 간단해야 하며 빠른 quicstart가 중요한 사용자 경험이다. UI는 다크모드를 기반으로 한 깔끔한 UI이다.

이름은 learn-linux
